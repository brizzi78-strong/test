{
  "name": "timeoff",
  "private": true,
  "version": "0.1.0",
  "description": "Time Off (PTO) — leave policies, accrual balances, and time-off requests with balance deduction and refund",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.8.5",
    "typescript": "~5.8.0"
  }
}
