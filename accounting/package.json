{
  "name": "accounting",
  "private": true,
  "version": "0.1.0",
  "description": "Accounting — small-business bookkeeping: chart of accounts, customers, invoices, payments, expenses, and P&L / A-R aging reports",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.8.5",
    "typescript": "~5.8.0"
  }
}
