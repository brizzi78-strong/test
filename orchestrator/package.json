{
  "name": "orchestrator",
  "private": true,
  "version": "0.1.0",
  "description": "Orchestrator — the shared-identity layer: one canonical company/person that cascades into every HR service and records the id map, so 'one employee record' resolves everywhere",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.8.5",
    "typescript": "~5.8.0"
  }
}
