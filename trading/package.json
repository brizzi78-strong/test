{
  "name": "trading",
  "private": true,
  "version": "0.1.0",
  "description": "Trading — a Robinhood-style commission-free brokerage engine: accounts, a deterministic mock market feed, market/limit orders, positions with average-cost basis, and a watchlist",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.8.5",
    "typescript": "~5.8.0"
  }
}
