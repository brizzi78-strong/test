{
  "name": "offboarding",
  "private": true,
  "version": "0.1.0",
  "description": "Offboarding — employee separation cases with a checklist (return equipment, revoke access, final pay, COBRA, exit interview) and derived status",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.8.5",
    "typescript": "~5.8.0"
  }
}
