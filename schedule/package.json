{
  "name": "schedule",
  "private": true,
  "version": "0.1.0",
  "description": "Live Schedule UI — a day-view scheduling app for a service business, backed by a backend-for-frontend that proxies to the Booking service",
  "type": "module",
  "engines": { "node": ">=22.18" },
  "scripts": { "start": "node src/index.ts", "test": "node --test", "typecheck": "tsc --noEmit" },
  "devDependencies": { "@types/node": "^22.8.5", "typescript": "~5.8.0" }
}
