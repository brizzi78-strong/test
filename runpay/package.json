{
  "name": "runpay",
  "private": true,
  "version": "0.1.0",
  "description": "Cardinal Payroll — an ADP-style Run Payroll console and employee pay-stub self-service, backed by a backend-for-frontend that proxies to the Payroll service",
  "type": "module",
  "engines": { "node": ">=22.18" },
  "scripts": { "start": "node src/index.ts", "test": "node --test", "typecheck": "tsc --noEmit" },
  "devDependencies": { "@types/node": "^22.8.5", "typescript": "~5.8.0" }
}
