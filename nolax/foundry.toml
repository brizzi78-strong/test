[profile.default]
src = "contracts"
test = "contracts"
out = "out"
libs = ["node_modules"]
solc_version = "0.8.28"

[profile.production]
optimizer = true
optimizer_runs = 200

# Dependencies are installed with npm (see package.json); remappings.txt
# points forge at node_modules so both toolchains share one dependency tree.
