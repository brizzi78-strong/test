# Caddy automatically obtains and renews a free HTTPS certificate for this domain
# (via Let's Encrypt) and forwards traffic to The Cardinal social app on port 4000.
__DOMAIN__ {
	encode gzip
	reverse_proxy 127.0.0.1:4000
}
